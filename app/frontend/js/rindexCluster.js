function createRIndexCluster(){
	//Empty Space
	node = document.getElementById("app");
	while (node.firstChild) {
		node.removeChild(node.firstChild);
	}
	
	node.innerHTML = "<h1>Soon Something RIndex-Cluster will appear here.</h1>";
		
	
};